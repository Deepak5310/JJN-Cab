rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Default deny: No public or unauthenticated read/write
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
